import { LocationExpandPipe } from './location-expand.pipe';

describe('LocationExpandPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationExpandPipe();
    expect(pipe).toBeTruthy();
  });
});
